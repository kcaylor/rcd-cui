# ==============================================
# CUI Audit Rules
# Purpose: Capture security-relevant events for CUI compliance.
# The rule set changes by zone to balance visibility and HPC performance.
# ==============================================
{% if audit_rules_profile == 'minimal' %}
-w /etc/passwd -p wa -k identity
-w /etc/group -p wa -k identity
-a always,exit -F arch=b64 -S sethostname -k system-locale
{% else %}
-w /etc/passwd -p wa -k identity
-w /etc/group -p wa -k identity
-w /etc/sudoers -p wa -k privileged-actions
-w /etc/ssh/sshd_config -p wa -k ssh-config
-a always,exit -F arch=b64 -S execve -C uid!=euid -F euid=0 -k privileged-actions
-a always,exit -F arch=b64 -S sethostname -S setdomainname -k system-locale
{% endif %}
