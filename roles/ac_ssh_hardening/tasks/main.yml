---
# WHY: Fail fast if zone context is missing so this control is never applied ambiguously.
- name: Validate CUI zone assignment for ac_ssh_hardening
  ansible.builtin.include_role:
    name: common
    tasks_from: validate_zone.yml
  tags: &role_tags
    - r2_3.1.12
    - r3_03.01.12
    - family_AC
    - zone_{{ cui_zone }}

# WHY: Install required software components that provide this security capability.
- name: Install required packages for ac_ssh_hardening
  ansible.builtin.package:
    name: "{{ ac_ssh_hardening_packages }}"
    state: present
  when:
    - ac_ssh_hardening_enabled | bool
    - ac_ssh_hardening_packages | length > 0
  tags: *role_tags

# WHY: Deploy controlled configuration from versioned templates for repeatable compliance state.
- name: Deploy configuration templates for ac_ssh_hardening
  ansible.builtin.template:
    src: "{{ item.src }}"
    dest: "{{ item.dest }}"
    owner: "{{ item.owner | default('root') }}"
    group: "{{ item.group | default('root') }}"
    mode: "{{ item.mode | default('0644') }}"
    validate: "{{ item.validate | default(omit) }}"
  loop: "{{ ac_ssh_hardening_templates }}"
  notify: Restart ac_ssh_hardening services
  when:
    - ac_ssh_hardening_enabled | bool
    - ac_ssh_hardening_templates | length > 0
  tags: *role_tags

# WHY: Place supporting helper scripts and policy files needed by this control implementation.
- name: Deploy static helper files for ac_ssh_hardening
  ansible.builtin.copy:
    src: "{{ item.src }}"
    dest: "{{ item.dest }}"
    owner: "{{ item.owner | default('root') }}"
    group: "{{ item.group | default('root') }}"
    mode: "{{ item.mode | default('0644') }}"
  loop: "{{ ac_ssh_hardening_files }}"
  notify: Restart ac_ssh_hardening services
  when:
    - ac_ssh_hardening_enabled | bool
    - ac_ssh_hardening_files | length > 0
  tags: *role_tags

# WHY: Ensure security services are continuously enforced after configuration changes.
- name: Ensure managed services are enabled and running for ac_ssh_hardening
  ansible.builtin.systemd:
    name: "{{ item.name }}"
    enabled: "{{ item.enabled }}"
    state: "{{ item.state }}"
  loop: "{{ ac_ssh_hardening_services }}"
  when:
    - ac_ssh_hardening_enabled | bool
    - ac_ssh_hardening_services | length > 0
  tags: *role_tags

# WHY: Mark verify-only controls so implementation runs are explicit and auditable.
- name: Report verify-only mode for ac_ssh_hardening when no stateful tasks apply
  ansible.builtin.debug:
    msg: "ac_ssh_hardening is configured as verify-heavy for this host and made no direct changes."
  changed_when: false
  when:
    - ac_ssh_hardening_enabled | bool
    - ac_ssh_hardening_packages | length == 0
    - ac_ssh_hardening_templates | length == 0
    - ac_ssh_hardening_files | length == 0
    - ac_ssh_hardening_services | length == 0
  tags: *role_tags
