# Hetzner Cloud Configuration for Demo Infrastructure
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# Get your API token from:
#   https://console.hetzner.cloud → Security → API Tokens → Generate API Token
#   Select "Read & Write" permissions

# Required: Hetzner Cloud API token
HCLOUD_TOKEN=

# SSH key for demo cluster access
# Default: infra/.ssh/demo_ed25519.pub (auto-generated on first run)
# The key is gitignored and never committed to the repository.
# TF_VAR_ssh_key_path=

# Optional: Override default region (default: hil = US West Hillsboro)
# TF_VAR_location=hil

# Optional: Override TTL threshold in hours (default: 4)
# TF_VAR_ttl_hours=4
